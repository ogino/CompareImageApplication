<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/activity_compare"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:cotext=".CompareActivity">

    <io.miyabi.image.compare.layout.CompareLayout
        android:id="@+id/compare_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:slider_thumb="@drawable/boundary"
        app:before_image="@drawable/before"
        app:after_image="@drawable/after"
    />


</FrameLayout>